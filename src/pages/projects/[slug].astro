---
// Phase 2: Individual project pages
// This file generates no pages until projects have full write-up content.
// To activate: add a `body` field to each project YAML and populate it,
// then implement getStaticPaths() below.

export async function getStaticPaths() {
  // Return empty array until Phase 2 - no pages generated
  return [];
}

const { slug } = Astro.params;
---

<!-- This page intentionally generates no routes in Phase 1 -->
